--- step [I] ---
Set hostname:

echo "archlinux" > /etc/hostname

Edit hosts file:

nano /etc/hosts

- Add the following lines:

127.0.0.1   localhost
::1         localhost
127.0.1.1   archlinux.localdomain archlinux

Next step: [J]
